import controller from './todo.controller';
import model from './todo.model';
import routes from './todo.routes';
import validation from './todo.validation';

export default { controller, model, routes, validation };
